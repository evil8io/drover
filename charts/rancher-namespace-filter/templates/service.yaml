apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher-namespace-filter.fullname" . }}
  labels:
    {{- include "rancher-namespace-filter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "rancher-namespace-filter.selectorLabels" . | nindent 4 }}
